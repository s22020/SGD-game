#include "core/game.h"

int main() {
    int windowWidth = 640;
    int windowHeight = 480;
    // 1. initialize game object with startGame method
    Game(windowWidth, windowHeight);
    return 0;
}
